image:
  name: makarius/isabelle:Isabelle2019
  entrypoint: [""]


compile:
  stage: build
  script:
    - /home/isabelle/Isabelle/bin/isabelle build -D . -j 10 -v
  artifacts:
    paths:
      - output/*.pdf
      - output/*.log
    when: always
    expire_in: 2 weeks
